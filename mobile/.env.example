# Copie para .env e ajuste para o seu ambiente.
# Em dev local com Expo Go + ngrok: use o subdomínio HTTPS do ngrok.
# Em dev local com emulador Android: use http://10.0.2.2:8000/api/
# Em dev local com simulador iOS:    use http://localhost:8000/api/
EXPO_PUBLIC_API_URL=https://monoxide-untagged-hull.ngrok-free.dev/api/
